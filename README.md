# Test GitHub Credential Manager

To test that your GitHub Credential Manager is setup properly, run the following
command in your terminal:

```sh
git clone https://github.com/appacademy/prepwork-test-gcm.git
```
